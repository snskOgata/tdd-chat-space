describe 'GET #index' do
    it "グループに紐ついた変数" do
      # user = create(:user)
      # group = create(:group)
      # get :index
      # expect(assigns(:group).name).to eq(group.name)
    end

    it "renders the :index template" do
      # get :index
      # expect(response).to render_template :index
    end
  end